extends layout.pug

block content
  main(role="main")
    section#jumbo(class="templatebg jumbosmall")
      h2(class="catheader")=profile.firstName

    section#articles(class="grid")
        article(class="grid12")
          h2= profile.firstName + " " +profile.lastName
          p= profile.bio
          h3 Posts
          each entry in posts
            a(href="/posts/"+entry.id)
              p= entry.title
